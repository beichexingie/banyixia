# 当前测试版问题梳理

## 已直接修复

1. 管理员与普通用户边界不清
   - 已加管理员路由保护
   - 非管理员不能再直接进入审核页

2. 当前用户地陪身份判断分散
   - 已把当前用户是否为地陪收敛到 `User` / `UserProvider`

3. 测试登录默认就是管理员
   - 已改为普通测试用户默认登录
   - 仅在调试环境保留管理员测试入口

4. 无效地陪仍可能进入下单页
   - 已拦截不存在或未认证地陪的下单入口

5. 封禁账号还能继续申请地陪
   - 已拦截

## 仍然存在、但需要产品或后端一起定的点

1. 角色体系还不完整
   - 当前只有普通用户 / 认证地陪 / 管理员
   - 还缺少更清晰的审核员、客服、财务等后台角色划分

2. 地陪资料与用户资料仍然部分混杂
   - 用户昵称、地陪展示名、真实姓名应严格分开
   - 用户资料页与地陪主页也应该拆得更彻底

3. 管理权限主要还是前端限制
   - 真实安全仍需要数据库 RLS 和服务端鉴权一起兜底

4. 订单流还偏测试态
   - 创建订单后只是进入待支付
   - 缺少支付成功后的完整状态推进、取消规则、退款规则、超时关闭规则

5. 地陪接单链路还不完整
   - 现在更像“用户直接下单”
   - 缺少接单确认、改价、拒单、超时未响应等节点

6. 聊天与交易边界还没完全打通
   - 缺少“先聊后下单”和“订单关联会话”的统一入口

7. 风控仍是轻量版本
   - 缺少实名校验 API
   - 缺少内容审核 API
   - 缺少异常行为监控

8. 客服与申诉链路还没闭环
   - 用户被封禁、申请被驳回、订单纠纷，缺少正式申诉入口

9. 后台能力仍偏单点
   - 当前只有地陪审核页
   - 后续应补订单后台、用户后台、举报后台、资金后台

10. 发布配置还没产品化
   - Android `applicationId` 仍是示例值
   - 正式包名、签名、环境区分仍需客户拍板

## 最建议下一批解决的事项

1. 明确正式角色体系
2. 明确订单状态机
3. 明确支付成功回调后的业务状态推进
4. 明确短信正式方案
5. 明确自建服务器的迁移优先级
