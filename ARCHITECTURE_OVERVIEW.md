# 当前测试版架构说明

## 1. 当前结构

1. `lib/config`
   - 统一放应用级配置
   - `app_config.dart` 作为外部资源入口
   - 地图、支付配置都从这里派生

2. `lib/bootstrap`
   - 启动初始化逻辑
   - 当前负责 Supabase 初始化

3. `lib/app`
   - App 根组件
   - Provider 装配
   - Router 装配

4. `lib/services`
   - 外部能力封装
   - 当前包括地图、支付、手机号登录等

5. `lib/providers`
   - 业务状态管理
   - 页面不直接操作底层 SDK，尽量通过 provider 和 service 间接访问

## 2. 当前测试版依赖

1. 数据与鉴权
   - Supabase

2. 地图能力
   - 高德 Web Service API

3. 支付能力
   - App 端支付宝 SDK
   - 后端下单参数目前可走 Supabase Edge Function
   - 也支持后续改成自建服务器 HTTP 接口

4. 登录验证码
   - 当前 Flutter 侧已经抽象为 `PhoneAuthService`
   - 目前默认实现仍是 Supabase Phone Auth
   - 后续可替换为自建服务器短信登录

## 3. 这次整理后的重点

1. 配置已统一入口
   - 不再把 Supabase、地图、支付关键配置散落在多个文件

2. 启动初始化已独立
   - 后续接正式环境或多环境时更容易扩展

3. 支付服务已改成统一 HTTP 后端入口
   - 后续把支付下单逻辑迁到云服务器时，App 端改动最小

4. 订单支付依赖已支持注入
   - 以后补微信支付或 mock 支付更方便

## 4. 后续迁移建议

1. 第一阶段
   - 继续用 Supabase 跑通业务
   - 把客户提供的正式 API 填齐

2. 第二阶段
   - 单独部署自建后端
   - 优先迁移支付、短信、地图代理、审核材料上传

3. 第三阶段
   - 视成本与可控性，再决定是否迁移数据库和鉴权

## 5. 最终理想形态

1. Flutter
   - 只负责页面、交互、设备能力、支付宝拉起

2. 自建后端
   - 负责支付下单、支付回调、短信验证码、业务校验、风控、管理接口

3. 数据层
   - 可继续用 Supabase，也可迁到自有数据库
